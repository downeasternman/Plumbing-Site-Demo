# Vercel AI Gateway (recommended). Create a key in the Vercel dashboard → AI Gateway,
# or use OIDC automatically when deployed to Vercel with AI Gateway enabled.
AI_GATEWAY_API_KEY=

# Optional: override the default model (Vercel AI Gateway model id, e.g. openai/gpt-4.1-mini).
# AI_MODEL=openai/gpt-4.1-mini
